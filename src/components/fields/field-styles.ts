/** Shared control styles for settings / form fields. */
export const fieldInputClass
  = 'min-h-11 w-full border border-surface-border bg-background px-3 py-2 text-sm text-foreground touch-manipulation disabled:opacity-50';

export const fieldLabelClass = 'text-sm font-medium text-foreground';

export const fieldHintClass = 'text-muted-foreground text-xs';

export const colorSwatchClass
  = 'h-11 w-12 cursor-pointer border border-surface-border bg-background touch-manipulation disabled:cursor-not-allowed disabled:opacity-50';

/** Comfortable tap target for checkbox rows on touch devices. */
export const fieldCheckRowClass
  = 'flex min-h-11 items-center gap-2 text-sm touch-manipulation';

/** Full-width range control with a larger vertical hit area. */
export const fieldRangeClass
  = 'accent-accent h-11 w-full touch-manipulation';
